node_modules/
# dist/ is committed so Vercel gets the pre-built output (better-sqlite3 rebuilt on Lambda)
.vite/

# SQLite database files — never commit; exposes demo data at rest
data.db
data.db-shm
data.db-wal
data.db-journal

.env
.env.*
!.env.example

*.log
.DS_Store
.vercel
server/generated/
.env*.local
